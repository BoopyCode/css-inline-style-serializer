# Style Snitch

A tiny JavaScript tool that makes CSS confess all its computed styles. Perfect for debugging when you're tired of clicking through dev tools. Just call `styleSnitch(yourElement)` or `console.snitch('selector')` and watch the styles spill their secrets.